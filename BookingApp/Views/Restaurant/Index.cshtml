@model IEnumerable<BookingApp.Models.RestaurantModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImgPath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ImgPath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.restaurantId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.restaurantId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.restaurantId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
